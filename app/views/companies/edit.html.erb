<h1>Editing company</h1>

<%= render 'form' %>

<%= button_to 'Show', @company, :method => :get, :class => "button_to" %> |
<%= button_to 'Back', companies_path, :method => :get, :class => "button_to" %>
